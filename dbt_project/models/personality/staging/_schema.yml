version: 2

models:
  - name: stg_customer_personalities
    description: Staging model for customer personalities seed data
    columns:
      - name: customer_id
        description: Primary key
        tests:
          - not_null
          - unique