version: 2

models:
  - name: fct_customer_personalities
    description: Fact model for customer personalities
    columns:
      - name: customer_id
        description: Primary key
        tests:
          - not_null
          - unique